# Core async and HTTP
aiohttp>=3.9.0
asyncio>=3.4.3

# Configuration and environment
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Retry logic and resilience
tenacity>=8.2.0

# GitHub integration
PyGithub>=2.3.0
gitpython>=3.1.40

# Logging (optional, stdlib logging is fine too)
loguru>=0.7.2

# For better datetime handling (optional but recommended)
python-dateutil>=2.8.2